{% extends 'YawAppBundle:Layouts:block_left_menu.html.twig' %}

{% block header %}My account{% endblock %}

{% block content %}
    {% if confirmation is defined %}
        {{ menu_userDashboard(true) }}
        <script type="text/javascript">
            $(function(){
                $('.col-left-nav').find('h2.heading').addClass('disabled');
            });
        </script>
    {% else %}
        {{ menu_userDashboard() }}
    {% endif %}
{% endblock %}